# Database URL — SQLite for local dev, swap to PostgreSQL for production
# SQLite:     DATABASE_URL="file:./dev.db"
# PostgreSQL: DATABASE_URL="postgresql://user:password@host:5432/obesetalk"
DATABASE_URL="file:./dev.db"
